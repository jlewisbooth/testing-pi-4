{
  "name": "communication-server",
  "version": "1.0.0",
  "description": "This server is the central part of all packets generated by each project. Here the packets are parsed and reformatted to then be sent onto a subscriber, i.e. the web server.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@ub/errors": "^1.0.2",
    "express": "^4.18.1",
    "express-ws": "^5.0.2",
    "redis": "^4.1.0"
  }
}
